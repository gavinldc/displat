package com.gc.nettyserver;

import com.gc.rpc.connection.Connection;

/**
 * 注册自己的handler
 * @author gavin
 *
 */
public class DataHandlerManager {

	//private static SingleHanlder handler=new SingleHanlder();
	
	public static void process(com.gc.rpc.packages.Package pack,Connection connection){
		//handler.process(pack, connection);
	}
	
}
